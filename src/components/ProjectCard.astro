---
const { title, image, description, link, delay} = Astro.props;
---

<div class="glass-effect rounded-lg p-6 hover-scale group relative overflow-hidden transition-all duration-300 shadow-none hover:shadow-[0_0_20px_5px_rgba(0,96,255,0.3)]" data-aos="fade-up" data-aos-delay={delay}>
    <img src={image} class="w-full h-48 object-cover rounded-lg mb-4" />
    <h3 class="text-2xl font-bold mb-4">{title}</h3>
    <p class="text-gray-400 mb-4">{description}</p>
    <a href={link} class="text-[var(--accent1)] hover:text-[var(--accent2)] transition-colors">View Project →</a>
</div>
