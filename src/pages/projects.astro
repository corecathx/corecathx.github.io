---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
import projects from "../data/projects.ts";
import ProjectCard from "../components/ProjectCard.astro";
---

<Layout title="Projects">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2
            class="mt-50 text-5xl md:text-6xl font-bold text-center w-fit mx-auto mb-12 gradient-text"
            data-aos="fade-up"
        >
            Projects
            <span class="block mt-2 font-normal text-xl md:text-2xl">
                I make stuff, here are some of them.
                <span class="block mt-2 font-normal text-sm md:text-md">
                    Everything in here is not final design-wise.
                </span>
            </span>
        </h2>

        <hr class="border-gray-800" />

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-6">
            {projects.map((project, index) => (
                <ProjectCard {...project} delay={index * 100} />
            ))}
        </div>

        <p class="gradient-text text-center my-10">... I'll try to list more of them later! (that is if i made another project)</p>
    </div>
</Layout>
